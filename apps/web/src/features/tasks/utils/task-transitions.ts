/**
 * Task workflow transition constants.
 * Single re-export point from generated model so components
 * never import generated types directly.
 */
import { TransitionTaskDtoTransition } from '@/api/generated/model';

export const TASK_TRANSITIONS = TransitionTaskDtoTransition;
