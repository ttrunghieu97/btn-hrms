/**
 * @deprecated Use `@/lib/date` instead (formatDateVN, formatDateTimeVN, etc.).
 * Kept for backward compatibility. New code should import from `@/lib/date`.
 */
export { formatDateVN as formatDate } from "./date";
