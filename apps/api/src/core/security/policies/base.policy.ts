/**
 * @deprecated Use policy-handler.interface.ts instead.
 * Kept for backward compatibility — re-exports the new interface under the old name.
 */
export type { PolicyHandler } from "./policy-handler.interface";
