meta {
  name: GET user permissions
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/{{userId}}/permissions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

vars:pre-request {
  userId: hieutt
}
