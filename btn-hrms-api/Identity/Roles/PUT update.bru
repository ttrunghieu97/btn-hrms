meta {
  name: PUT update
  type: http
}

put {
  url: {{base_url}}/api/v1/roles/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"name":"{{role_name}}"}
}
