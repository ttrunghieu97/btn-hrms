meta {
  name: PUT
  type: http
}

put {
  url: {{base_url}}/api/v1/qualifications
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"employeeId":"{{eid}}","qualifications":[]}
}
