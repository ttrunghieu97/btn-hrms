meta {
  name: POST create template
  type: http
}

post {
  url: {{base_url}}/api/v1/scheduling/templates
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"name":"Morning","startTime":"06:00"}
}
