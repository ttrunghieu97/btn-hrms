meta {
  name: PATCH update assignment
  type: http
}

patch {
  url: {{base_url}}/api/v1/scheduling/assignments/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"templateId":"{{tid}}"}
}
