meta {
  name: POST create
  type: http
}

post {
  url: {{base_url}}/api/v1/positions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"title":"{{title}}","code":"{{code}}"}
}
