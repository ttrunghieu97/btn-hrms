meta {
  name: PATCH update
  type: http
}

patch {
  url: {{base_url}}/api/v1/positions/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"title":"{{title}}"}
}
