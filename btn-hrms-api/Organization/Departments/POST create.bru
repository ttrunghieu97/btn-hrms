meta {
  name: POST create
  type: http
}

post {
  url: {{base_url}}/api/v1/departments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"name":"{{name}}","code":"{{code}}"}
}
