meta {
  name: PUT update
  type: http
}

put {
  url: {{base_url}}/api/v1/departments/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"name":"{{name}}"}
}
