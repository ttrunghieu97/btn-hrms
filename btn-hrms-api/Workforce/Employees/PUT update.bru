meta {
  name: PUT update
  type: http
}

put {
  url: {{base_url}}/api/v1/employees/{{id}}
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:multipart-form {
  fullName: {{name}}
}
