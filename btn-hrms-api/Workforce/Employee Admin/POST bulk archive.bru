meta {
  name: POST bulk archive
  type: http
}

post {
  url: {{base_url}}/api/v1/employees/bulk/archive
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"employeeIds":["a"]}
}
