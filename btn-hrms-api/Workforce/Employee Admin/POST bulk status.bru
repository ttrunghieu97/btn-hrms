meta {
  name: POST bulk status
  type: http
}

post {
  url: {{base_url}}/api/v1/employees/bulk/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"employeeIds":["a"],"status":"inactive"}
}
