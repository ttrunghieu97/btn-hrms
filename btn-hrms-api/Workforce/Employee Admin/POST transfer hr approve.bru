meta {
  name: POST transfer hr approve
  type: http
}

post {
  url: {{base_url}}/api/v1/employees/{{id}}/transfers/{{iid}}/hr-approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}
