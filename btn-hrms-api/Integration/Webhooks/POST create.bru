meta {
  name: POST create
  type: http
}

post {
  url: {{base_url}}/api/v1/integrations/webhooks
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"url":"https://ex.com","events":["emp.created"]}
}

