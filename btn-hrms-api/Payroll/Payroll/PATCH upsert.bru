meta {
  name: PATCH upsert
  type: http
}

patch {
  url: {{base_url}}/api/v1/payroll/{{eid}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"basicSalary":15000000}
}
