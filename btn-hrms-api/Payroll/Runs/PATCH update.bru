meta {
  name: PATCH update
  type: http
}

patch {
  url: {{base_url}}/api/v1/payroll-runs/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"name":"v2"}
}
