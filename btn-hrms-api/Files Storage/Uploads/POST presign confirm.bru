meta {
  name: POST presign confirm
  type: http
}

post {
  url: {{base_url}}/api/v1/uploads/presign/confirm
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"uploadId":"{{uid}}"}
}
