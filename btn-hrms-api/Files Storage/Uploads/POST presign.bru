meta {
  name: POST presign
  type: http
}

post {
  url: {{base_url}}/api/v1/uploads/presign
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"fileName":"doc.pdf"}
}
