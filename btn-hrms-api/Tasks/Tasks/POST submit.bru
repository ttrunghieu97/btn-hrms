meta {
  name: POST submit
  type: http
}

post {
  url: {{base_url}}/api/v1/tasks/{{id}}/submit
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"note":"Done"}
}
