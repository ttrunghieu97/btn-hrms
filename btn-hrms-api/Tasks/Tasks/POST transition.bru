meta {
  name: POST transition
  type: http
}

post {
  url: {{base_url}}/api/v1/tasks/{{id}}/transitions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"action":"start"}
}
