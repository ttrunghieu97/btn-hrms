meta {
  name: PATCH reject
  type: http
}

patch {
  url: {{base_url}}/api/v1/tasks/{{id}}/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"reason":"No"}
}
