meta {
  name: PATCH resolve exception
  type: http
}

patch {
  url: {{base_url}}/api/v1/exceptions/{{id}}/resolve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"resolution":"approved"}
}
