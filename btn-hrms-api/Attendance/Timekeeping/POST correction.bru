meta {
  name: POST correction
  type: http
}

post {
  url: {{base_url}}/api/v1/corrections
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"employeeId":"{{eid}}","date":"2026-07-04"}
}
