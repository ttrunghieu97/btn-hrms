meta {
  name: POST reject
  type: http
}

post {
  url: {{base_url}}/api/v1/approval/inbox/{{id}}/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"note":"No"}
}
