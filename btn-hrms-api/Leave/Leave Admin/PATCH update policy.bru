meta {
  name: PATCH update policy
  type: http
}

patch {
  url: {{base_url}}/api/v1/leave-admin/policies/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"name":"Updated"}
}
