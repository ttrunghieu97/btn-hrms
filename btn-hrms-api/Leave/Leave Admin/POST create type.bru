meta {
  name: POST create type
  type: http
}

post {
  url: {{base_url}}/api/v1/leave-admin/types
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"name":"Sick","code":"sick"}
}
