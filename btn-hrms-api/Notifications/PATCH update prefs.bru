meta {
  name: PATCH update prefs
  type: http
}

patch {
  url: {{base_url}}/api/v1/platform-notifications/preferences
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"email":true}
}
