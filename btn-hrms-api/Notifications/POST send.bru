meta {
  name: POST send
  type: http
}

post {
  url: {{base_url}}/api/v1/platform-notifications/send
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"userId":"{{uid}}","templateId":"{{tid}}"}
}
