meta {
  name: PATCH update template
  type: http
}

patch {
  url: {{base_url}}/api/v1/platform-notifications/templates/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {"name":"UPD"}
}
